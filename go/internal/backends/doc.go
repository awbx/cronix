// Package backends defines the Backend interface that every host
// scheduler adapter (crontab, systemd-timer, kubernetes) implements.
//
// Phase 4 introduces the interface; Phase 5 ships the adapters.
package backends
