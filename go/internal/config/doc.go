// Package config loads and validates the operator config (cronix.yaml):
// manifest sources, secret references, lock backend choice, log level,
// default policies. Phase 4 populates this package.
package config
