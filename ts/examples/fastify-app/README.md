# Fastify example

Wires `@awbx/cronix-sdk` into Fastify. Same pattern as the Express example — two routes calling `cron.verify()`.

```bash
pnpm install
pnpm --filter @cronix-example/fastify-app dev
```
